<div>
  <h1 class = "welcome_user">Welcome <%= current_user.username %> </h1>
<div>
<% Game.all.each do |game| %> 
  <% @game = game %>
  <% if current_user == game.user %>
    <%= render 'games/show' %>
  <% end %>
<% end %>
</div>
</div>